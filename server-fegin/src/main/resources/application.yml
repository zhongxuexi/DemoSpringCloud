server:
  port: 8201
  tomcat:
    max-threads: 50
spring:
  application:
    name: server-order-fegin
eureka:
  client:
    service-url:
      defaultZone: http://server1:8101/eureka/
feign:
  hystrix:
    enabled: true
hystrix:
  command:
    default:
      execution:
        isolation:
            thread:
             timeoutInMilliseconds: 2000

